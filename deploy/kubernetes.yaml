apiVersion: apps/v1
kind: Deployment
metadata: {name: ai-feature-store}
spec:
 replicas: 2
 selector: {matchLabels: {app: ai-feature-store}}
 template:
  metadata: {labels: {app: ai-feature-store}}
  spec:
   containers:
   - name: api
     image: ai-feature-store:latest
     ports: [{containerPort: 8000}]
     readinessProbe: {httpGet: {path: /health/ready, port: 8000}}
     livenessProbe: {httpGet: {path: /health/live, port: 8000}}
