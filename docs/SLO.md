# SLO / SLI

SLIs: p50/p95/p99 latency, throughput, error rate, concurrency/saturation, dependency health and resource usage.

Baseline: p95 online read < 100 ms; feature error rate < 1%; stale-feature rate is measured.

Reference target; calibrate with production traffic.